{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "game-app.serviceAccountName" . }}
  labels:
    {{- include "game-app.labels" . | nindent 4 }}
{{- end }}
