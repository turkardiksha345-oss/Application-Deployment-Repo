apiVersion: v1
kind: Service
metadata:
  name: {{ include "game-app.fullname" . }}-backend
  labels:
    {{- include "game-app.labels" . | nindent 4 }}
    component: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "game-app.selectorLabels" . | nindent 4 }}
    component: backend
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "game-app.fullname" . }}-frontend
  labels:
    {{- include "game-app.labels" . | nindent 4 }}
    component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "game-app.selectorLabels" . | nindent 4 }}
    component: frontend
